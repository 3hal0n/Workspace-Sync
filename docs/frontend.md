Frontend (Next.js + Tailwind)
Since you are doing this without AI, your goal is pixel-perfection.

Strict Layout: Create a Layout component that persists the Sidebar across all pages.

Data Fetching: Use TanStack Query (React Query) with standard fetch.

Why? Most Java-based companies use REST APIs. Showing you can manage cache and loading states with React Query is a massive "Green Flag" for interviewers.

Responsive State: Implement the mobile view from the Figma. Ensure the sidebar becomes a hamburger menu on small screens.